\chapter{Results}
\label{chap:results}

\paragraph{Data pre-processing.}
Prior to analysis, runs with a duration exceeding 600 seconds (10 minutes) were excluded
as anomalies. These correspond to timed-out or otherwise pathological executions whose
resource measurements would distort the distribution of duration and token statistics.
All results reported below are computed on the filtered dataset.

% -------------------------------------------------------
\section{Quality Metrics}
% -------------------------------------------------------

\subsection{DeepEval: Correctness, Relevancy, and Completeness}

The joint distribution of correctness, relevancy, and completeness (defined in Section~\ref{sec:metrics}) across all queries and sessions is shown in Figure~\ref{fig:deepeval-pairplot}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/deep_eval_pairplot.png}
    \caption{Pairplot of DeepEval quality metrics (correctness, relevancy, completeness).
             Diagonal panels show per-metric density estimates. The custom agent (orange)
             exhibits higher correctness and completeness, while relevancy is similarly
             high for both configurations.}
    \label{fig:deepeval-pairplot}
\end{figure}

Both distributions are left-skewed across all three metrics, with density concentrated
near 1.0 --- but the degree of concentration differs substantially. The custom agent's
distributions are more tightly clustered near the upper end, while the baseline's are
more spread, with a heavier low-scoring tail. This confirms that the custom agent's
advantage is not driven by a small number of high-scoring queries but reflects a broad
shift in the distribution. Relevancy scores are highly concentrated near 1.0 for both
configurations --- as long as an agent attempts to answer the question, the response
tends to be on-topic regardless of architecture --- though the baseline's low-scoring
tail corresponds primarily to cases where retrieval fails entirely. Completeness follows
the same pattern: both are left-skewed, but the custom agent's distribution is more
concentrated near 1.0, while the baseline exhibits more low-completeness responses.
Overall, the pairplot reveals greater consistency and reliability in the custom agent's
responses across all three quality dimensions.

\bigbreak

Session-level trajectories tell the same story longitudinally (Figure~\ref{fig:deepeval-lineplot}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/deep_eval_lineplot.png}
    \caption{DeepEval quality metrics across sessions for each agent configuration.}
    \label{fig:deepeval-lineplot}
\end{figure}

Both agents broadly track each other through the session progression, but the custom
agent remains consistently above the baseline throughout. The shaded bands represent
95\% confidence intervals across the three repeated runs; the narrower bands on the
custom agent's trajectory reflect a tighter spread across runs, consistent with the
lower score variance reported in the statistical analysis. This session-level
consistency reinforces the pairplot finding that the quality advantage is broad and
reliable rather than driven by occasional strong queries.

\bigbreak

Breaking the correctness trajectory down by dataset (Figure~\ref{fig:lineplot-correctness-by-dataset})
reveals that the performance gap is not uniform across cases.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/lineplot_correctness_by_dataset.png}
    \caption{Correctness over session progression, split by dataset.}
    \label{fig:lineplot-correctness-by-dataset}
\end{figure}

On TOSL-2024-125319 --- the fully authentic case with the larger document volume and greater
factual complexity --- the custom agent's advantage grows more noticeably as sessions
accumulate. On THRD-2021-163881, the custom agent also leads, but the gap is smaller and more
stable. This pattern is consistent with the architectural hypothesis: the factsheet and
structured retrieval tools confer the greatest benefit precisely when the case is large
and complex, where naive RAG retrieval is most prone to missing relevant facts dispersed
across many documents.

\bigbreak

Figure~\ref{fig:deepeval-violin} shows the same comparison as a violin plot, making the distributional differences and interquartile ranges directly visible.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/deep_eval_violin.png}
    \caption{Violin plot of DeepEval quality metrics (correctness, relevancy, completeness) by agent configuration.
             The custom agent exhibits narrower distributions with higher medians across all three metrics.}
    \label{fig:deepeval-violin}
\end{figure}

\bigbreak

Figure~\ref{fig:deepeval-success-dist} shows the distribution of binary pass/fail outcomes across all queries.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/deep_eval_success_distribution.png}
    \caption{Distribution of pass/fail outcomes across all queries for each agent configuration,
             based on the DeepEval threshold. The custom agent achieves a higher pass rate.}
    \label{fig:deepeval-success-dist}
\end{figure}

\bigbreak

The aggregate score distribution across all DeepEval metrics is shown in Figure~\ref{fig:deepeval-distribution}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/deepeval_distribution.png}
    \caption{Aggregate distribution of all DeepEval metric scores. The custom agent's distribution
             is more concentrated near the upper end, while the baseline exhibits a heavier low-scoring tail.}
    \label{fig:deepeval-distribution}
\end{figure}

\bigbreak

The model-level breakdown (Figure~\ref{fig:deepeval-barplot-llms}) adds a further dimension.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/deep_eval_barplot_llms.png}
    \caption{DeepEval quality metrics broken down by underlying LLM. The custom agent
             outperforms the baseline for every model tested, with the gap narrowing for
             the most capable model.}
    \label{fig:deepeval-barplot-llms}
\end{figure}

The custom agent outperforms the baseline across all three metrics for every model
tested, with one exception: for the most capable model in the evaluation, the two
configurations converge and the difference becomes negligible. For the smaller and
less capable models, the margin is wider. This suggests that structured factsheet
context acts as a capability amplifier whose marginal effect is greatest when the
base model has limited capacity to compensate for noisy or incomplete retrieval.
The implications of this model-dependent effect are discussed further in
Chapter~\ref{chap:discussion}.


\paragraph{Dataset-level breakdown.}
Table~\ref{tab:results} reports per-model scores for both agent configurations across
each dataset. Table~\ref{tab:diff_results_pct} condenses this into mean custom-minus-baseline
differences, and Table~\ref{tab:diff_variance} reports the variance of that gap across LLMs.

\begin{table}[htbp]
\centering
\caption{Mean completeness, correctness, and relevancy by dataset, model, and agent
         configuration (Baseline+RAG vs.\ Custom).}
\label{tab:results}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llrrrrrr}
\toprule
 & & \multicolumn{2}{c}{\textbf{Completeness}} & \multicolumn{2}{c}{\textbf{Correctness}} & \multicolumn{2}{c}{\textbf{Relevancy}} \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}\cmidrule(lr){7-8}
\textbf{Dataset} & \textbf{Model} & \textbf{Base} & \textbf{Cust} & \textbf{Base} & \textbf{Cust} & \textbf{Base} & \textbf{Cust} \\
\midrule
THRD-2021-163881 & GLM-5            & 0.880 & 0.921 & 0.890 & 0.928 & 0.992 & 1.000 \\
THRD-2021-163881 & Qwen3.5-397B     & 0.937 & 0.947 & 0.927 & 0.944 & 0.996 & 1.000 \\
THRD-2021-163881 & Gemini 2.5 Flash & 0.740 & 0.853 & 0.665 & 0.831 & 0.996 & 0.979 \\
THRD-2021-163881 & Gemini 2.5 Pro   & 0.903 & 0.870 & 0.894 & 0.871 & 1.000 & 1.000 \\
\midrule
TOSL-2024-125319 & GLM-5            & 0.992 & 0.982 & 0.971 & 0.956 & 1.000 & 1.000 \\
TOSL-2024-125319 & Qwen3.5-397B     & 0.700 & 0.926 & 0.727 & 0.927 & 0.733 & 1.000 \\
TOSL-2024-125319 & Gemini 2.5 Flash & 0.822 & 0.911 & 0.718 & 0.889 & 0.967 & 0.980 \\
TOSL-2024-125319 & Gemini 2.5 Pro   & 0.956 & 0.956 & 0.911 & 0.924 & 0.998 & 1.000 \\
\bottomrule
\end{tabular}}
\end{table}

\begin{table}[htbp]
\centering
\caption{Mean difference (custom $-$ baseline) per dataset, in percentage points.
         Positive values indicate the custom agent scores higher.}
\label{tab:diff_results_pct}
\begin{tabular}{lrrr}
\toprule
\textbf{Dataset} & \textbf{Completeness} & \textbf{Correctness} & \textbf{Relevancy} \\
\midrule
THRD-2021-163881     & $+3.27$\% & $+4.96$\% & $-0.10$\% \\
TOSL-2024-125319-MIN & $+7.60$\% & $+9.24$\% & $+7.06$\% \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Variance of the per-model custom$-$baseline difference per dataset, expressed
         as a percentage ($\times 100$). Higher variance indicates less uniform gains
         across LLMs.}
\label{tab:diff_variance}
\begin{tabular}{lrrr}
\toprule
\textbf{Dataset} & \textbf{Completeness} & \textbf{Correctness} & \textbf{Relevancy} \\
\midrule
THRD-2021-163881     & $0.38$\% & $0.67$\% & $0.01$\% \\
TOSL-2024-125319-MIN & $1.20$\% & $1.18$\% & $1.71$\% \\
\bottomrule
\end{tabular}
\end{table}

The dataset-level breakdown reveals a consistent asymmetry: the custom agent's advantage
is substantially larger on TOSL-2024-125319 than on THRD-2021-163881 across all three quality
dimensions. On THRD-2021-163881, the mean correctness gain is $+4.96$ percentage points; on
TOSL-2024-125319 it more than doubles to $+9.24$ pp. Completeness follows the same pattern
($+3.27$ vs.\ $+7.60$ pp), while relevancy is effectively neutral on THRD-2021-163881 ($-0.10$
pp) but shows a clear positive gain on TOSL-2024-125319 ($+7.06$ pp).

This asymmetry is consistent with the architectural hypothesis. TOSL-2024-125319 is the fully
authentic dataset with a larger and more heterogeneous document set; queries on this case
require cross-document synthesis that naive RAG is most prone to fail. The factsheet's
structured representation provides the greatest marginal benefit precisely where
retrieval-only approaches are most likely to miss facts dispersed across many documents.

The variance table reinforces this interpretation. The variance of the per-model gap is
uniformly higher on TOSL-2024-125319 than on THRD-2021-163881 across all metrics. This indicates that
the advantage is less uniform across LLMs on the more complex dataset: some models
benefit more from the structured context than others, and this interaction is amplified
when the case is harder. The notable exception is gemini-2.5-pro on TOSL-2024-125319, where the
baseline already achieves high scores, leaving less headroom for improvement.


% -------------------------------------------------------
\subsection{Query-Type Breakdown}
\label{sec:qtype-results}
% -------------------------------------------------------

A breakdown of quality metrics by query type provides the most direct empirical
evidence for RQ1. As defined in Section~\ref{sec:dataset-limitations}, queries are
classified as either \emph{factual} (specific, enumerable facts with a bounded correct
answer) or \emph{summary} (cross-document synthesis requiring a narrative overview).
Summary queries, by their nature, require the agent to synthesise information across
multiple documents and sessions --- precisely the capability that naive RAG struggles
with most.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/deep_eval_barplot_qtype.png}
    \caption{DeepEval quality metrics broken down by query type (factual vs.\ summary).
             The custom agent's advantage is substantially larger on summary queries,
             which require cross-document synthesis, than on factual queries.}
    \label{fig:deepeval-barplot-qtype}
\end{figure}

\begin{table}[H]
\centering
\caption{Mean correctness, relevancy, and completeness by query type and agent
         configuration. Completeness is not evaluated on summary queries
         (see Section~\ref{sec:dataset-limitations}).}
\label{tab:qtype-results}
\resizebox{0.85\textwidth}{!}{%
\begin{tabular}{llrrr}
\toprule
\textbf{Query Type} & \textbf{Configuration} & \textbf{Correctness} & \textbf{Relevancy} & \textbf{Completeness} \\
\midrule
Factual & Baseline+RAG & 0.882 & 0.996 & 0.910 \\
Factual & Custom       & 0.901 & 0.993 & 0.924 \\
\midrule
Summary & Baseline+RAG & 0.775 & 0.912 & --- \\
Summary & Custom       & 0.919 & 0.999 & --- \\
\bottomrule
\end{tabular}}
\end{table}

The pattern is striking. On factual queries, the custom agent leads by 1.9 percentage
points in correctness --- a modest but consistent margin. On summary queries, the gap
widens to 14.4 percentage points ($0.919$ vs.\ $0.775$). The same asymmetry holds for
relevancy: the baseline's relevancy on summary queries ($0.912$) is substantially below
its factual query relevancy ($0.996$), reflecting cases where retrieval fails to surface
the distributed context required for a coherent synthesis and the agent produces an
incomplete or unfocused response. The custom agent's relevancy on summary queries
($0.999$) is effectively at ceiling.

Mann-Whitney U tests confirm that the custom agent's advantage on summary queries is
statistically significant: correctness ($U = 12{,}813$, $p = 9.3 \times 10^{-3}$) and
relevancy ($U = 12{,}542$, $p = 2.9 \times 10^{-5}$). Since summary queries require
cross-document synthesis by definition --- they cannot be answered from a single
retrieved chunk --- this constitutes direct empirical support for RQ1.

% -------------------------------------------------------
\subsection{BERTScore}
% -------------------------------------------------------

BERTScore and Sentence-BERT similarity (defined in Section~\ref{sec:metrics}) serve as a cross-validation signal that is independent of the LLM judge used for the DeepEval metrics. Because BERTScore measures surface-level semantic overlap, it is less sensitive to structured factual completeness than the DeepEval metrics: a response can score well on BERTScore while omitting specific facts that correctness and completeness evaluation would penalise. Agreement between the two metric families therefore provides stronger evidence that observed quality differences reflect genuine architectural effects rather than judge artefacts.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/bert_pairplot.png}
    \caption{Pairplot of BERTScore sub-metrics (precision, recall, F1, and sentence-BERT
             similarity). Distributions for the two agent configurations largely overlap,
             indicating comparable surface-level semantic quality.}
    \label{fig:bert-pairplot}
\end{figure}

The distributions have considerable overlap between the two agent types, but the custom
agent shows a higher peak with less variation and more consistent surface-level semantic
similarity to the reference answers. The baseline has a wider spread with more
low-scoring outliers, suggesting it occasionally produces responses less semantically
aligned with the reference. The near-identical BERTScore distributions complement rather
than contradict the DeepEval findings: both agents produce semantically coherent output,
but the custom agent more reliably includes the specific factual content the gold-standard
reference requires.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/bert_lineplot.png}
    \caption{BERTScore F1 and sentence-BERT similarity across sessions. Trajectories are
             flat and closely aligned between configurations, with the custom agent showing
             marginally narrower confidence intervals.}
    \label{fig:bert-lineplot}
\end{figure}

Across sessions, BERTScore F1 and sentence-BERT similarity are relatively flat and
closely aligned for both agent types, with only minor fluctuations. This is expected:
surface similarity to reference answers does not change systematically as the case grows,
since the phrasing of correct responses does not become more or less reference-like with
document volume. The session-level divergence observed in the DeepEval metrics is
therefore not an artefact of reference phrasing but reflects genuine differences in
factual coverage.

% -------------------------------------------------------
\subsection{Resource Metrics}
% -------------------------------------------------------

Resource metrics (defined in Section~\ref{sec:metrics}) are analysed alongside quality scores to determine whether the custom agent's architectural overhead is justified by its performance gains.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/resource_pairplot.png}
    \caption{Pairplot of resource metrics (duration in seconds, input tokens, output tokens,
             total tokens, and LLM call count). Duration and token distributions overlap
             substantially between configurations; the main visible difference is a
             more right-skewed LLM call count for the custom agent.}
    \label{fig:resource-pairplot}
\end{figure}

Figure~\ref{fig:resource-pairplot} shows that the two configurations are broadly
similar across duration, input tokens, output tokens, and total tokens --- the
distributions overlap substantially with no visually clear separation. The most
notable difference is in LLM call count: the custom agent's distribution is more
right-skewed, reflecting occasional queries that require additional tool calls to
surface structured case facts, while the baseline is more tightly peaked in the
lower range (approximately 2--5 calls per query). This is consistent with the custom
agent's richer tool set, which can trigger extra retrieval steps when the factsheet
and vector store are both consulted within a single query.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/resource_lineplot.png}
    \caption{Resource metrics across sessions. Token counts and latencies fluctuate
             without a systematic trend for either configuration, and the two track each
             other closely throughout.}
    \label{fig:resource-lineplot}
\end{figure}

Figure~\ref{fig:duration-vs-correctness} plots inference duration against correctness
score for individual queries. The custom agent's responses cluster more tightly ---
higher correctness with less spread in duration --- while the baseline is more
dispersed. The pattern confirms that longer inference time does not produce higher
quality: both configurations occupy similar latency ranges, yet the custom agent
achieves consistently higher scores within that range.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/duration_vs_correctness.png}
    \caption{Scatter plot of inference duration (seconds) against correctness score per query.
             The custom agent's responses are more tightly concentrated at higher correctness
             scores. No positive correlation between latency and quality is observed for
             either configuration.}
    \label{fig:duration-vs-correctness}
\end{figure}

\bigbreak

Unlike the quality metrics, no consistent trend is visible in the resource trajectories
over session progression. Token counts and latencies fluctuate without a systematic
increase, and the two configurations track each other closely throughout
(Figure~\ref{fig:resource-lineplot}). The absence of a growth trend is partly explained
by the context compression mechanisms applied to the custom agent: as the factsheet grows,
significance filtering and display shortening keep the payload from expanding
proportionally.

\paragraph{Cost analysis.}
Absolute token counts are not directly comparable across datasets with different document
volumes and session lengths. The total cost for TOSL-2024-125319 is substantially higher than for THRD-2021-163881, making direct comparison of absolute costs misleading. 
To enable model-level cost comparisons, each run's estimated cost (input tokens $\times$ price-per-token $+$ output tokens $\times$ price-per-token)
is normalised to a percentile rank within its dataset:
%
\begin{equation*}
  \texttt{rank\_cost} = \text{rank}_{\text{pct}}\!\left(\text{total\_cost}\right)
  \quad \text{grouped by dataset}
\end{equation*}
%
A rank of 1.0 means the most expensive configuration within that dataset; 0.0 means the
cheapest. This makes cost comparable across datasets of different scales without requiring
absolute price estimates for every provider.

Figures~\ref{fig:cost-vs-correctness}--\ref{fig:cost-vs-completeness} plot cost rank
against each quality metric for individual queries.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/cost_vs_correctness.png}
    \caption{Estimated relative cost rank vs.\ correctness score per query. The custom
             agent (orange) occupies the upper-left region for several models, achieving
             higher correctness at lower or equal relative cost.}
    \label{fig:cost-vs-correctness}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/cost_vs_relevancy.png}
    \caption{Estimated relative cost rank vs relevancy score per query. Relevancy is
             less sensitive to architecture than correctness: both configurations cluster
             near the top of the relevancy scale regardless of cost, reflecting that
             on-topic responses are easy to achieve once retrieval succeeds.}
    \label{fig:cost-vs-relevancy}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/cost_vs_completeness.png}
    \caption{Estimated relative cost rank vs.\ completeness score per query. The custom
             agent dominates the upper portion of the completeness axis at comparable
             or lower cost, mirroring the correctness pattern.}
    \label{fig:cost-vs-completeness}
\end{figure}

The correctness and completeness scatter plots reveal a consistent pattern: the custom
agent (orange) occupies the upper portion of both axes at comparable or lower relative
cost for most models. The most striking case is \texttt{gemini-2.5-flash}, where the
custom configuration achieves a cost rank of 0.125 (cheapest in the set) while delivering
a correctness score of 0.860, versus the baseline's rank of 0.250 and score of 0.691.
Relevancy, by contrast, is less discriminative: both configurations cluster near 1.0
regardless of cost, consistent with the earlier finding that on-topic responses are
easy to achieve once retrieval succeeds at all.

Table~\ref{tab:cost-quality} summarises mean cost rank and quality scores per model and
agent configuration.

\begin{table}[htbp]
\centering
\caption{Mean relative cost rank and quality scores per model and agent configuration.
         Cost rank is a within-dataset percentile ($0 =$ cheapest, $1 =$ most expensive).}
\label{tab:cost-quality}
\begin{tabular}{llrrrr}
\toprule
\textbf{Model} & \textbf{Agent} & \textbf{Cost rank} & \textbf{Correctness} & \textbf{Relevancy} & \textbf{Completeness} \\
\midrule
GLM-5             & baseline\_rag & 0.875 & 0.930 & 0.996 & 0.936 \\
GLM-5             & custom        & 1.000 & 0.942 & 1.000 & 0.951 \\
Qwen3.5-397B      & baseline\_rag & 0.438 & 0.827 & 0.865 & 0.818 \\
Qwen3.5-397B      & custom        & 0.438 & 0.935 & 1.000 & 0.936 \\
gemini-2.5-flash  & baseline\_rag & 0.250 & 0.691 & 0.981 & 0.781 \\
gemini-2.5-flash  & custom        & 0.125 & 0.860 & 0.980 & 0.882 \\
gemini-2.5-pro    & baseline\_rag & 0.688 & 0.902 & 0.999 & 0.929 \\
gemini-2.5-pro    & custom        & 0.688 & 0.898 & 1.000 & 0.913 \\
\bottomrule
\end{tabular}
\end{table}

The table highlights the practical trade-off. For three of the four models tested,
the custom agent achieves equal or lower cost rank while delivering meaningfully higher
correctness and completeness. \texttt{gemini-2.5-pro} is the exception: both
configurations share the same cost rank and their quality scores are within one
percentage point, confirming that the most capable model compensates for the absence
of structured context through its own reasoning. The structured factsheet therefore
functions most effectively as a cost-efficient capability amplifier for mid-tier models.

\paragraph{Actual vs. Advertised Cost.}
A further nuance emerges when examining the relationship between advertised input token price and the actual, usage-weighted input cost. The advertised price per token does not fully predict the total cost of a query, because more capable models may use tools more efficiently, requiring fewer tokens overall to arrive at an answer. Figure~\ref{fig:actual-input-cost} illustrates this by plotting the ranked actual input cost against the advertised price.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/actual_input_cost.png}
    \caption{Actual input cost (ranked) vs. advertised input price per token. The diagonal reference line represents perfect proportionality — if advertised price fully predicted actual cost, all points would lie on this line. The non-linear deviation indicates that more capable models can be more token-efficient.}
    \label{fig:actual-input-cost}
\end{figure}

The relationship is notably non-linear. The open-source models, despite having competitive advertised prices, tend to incur a higher relative \texttt{actual\_input\_cost\_rank}. 
Conversely, the closed-source models demonstrate greater token efficiency. \texttt{gemini-2.5-pro}, for instance, shows a significant drop in actual cost rank compared to its position on the price axis, 
performing on par with models that are nominally cheaper per token. \texttt{gemini-2.5-flash} also performs well on this metric, delivering a low actual cost. 
This finding, while secondary to the main research questions, is a valuable insight for practical deployment: the true cost of operating a model is a function of both its price 
and its underlying capability to reason efficiently and use tools judiciously. The custom agent architecture does not significantly alter this dynamic, as its primary contribution is to the quality and consistency of the context, 
not the model's intrinsic reasoning efficiency.
% -------------------------------------------------------

\section{Statistical Modeling}
% -------------------------------------------------------

\subsection{Mann-Whitney U Test}

Hypothesis tests follow the design described in Section~\ref{sec:metrics}. Normality was confirmed to be absent via Shapiro-Wilk ($p < 10^{-18}$ for every metric tested), validating the use of the one-sided Mann-Whitney U test as described in Chapter~\ref{chap:method}.

\paragraph{Quality metrics.}
Table~\ref{tab:mann-whitney} summarises the results. Correctness and relevancy both show
statistically significant advantages for the custom agent at the $\alpha = 0.05$ level.
Completeness does not reach significance, though the directional trend is the same.

\begin{table}[H]
\centering
\caption{Mann-Whitney U test results for quality metrics (one-sided, $H_1$: custom $>$ baseline).}
\label{tab:mann-whitney}
\begin{tabular}{lrrl}
\hline
\textbf{Metric} & \textbf{U-statistic} & \textbf{p-value} & \textbf{Result} \\
\hline
Correctness  & 74{,}780.50 & $1.03 \times 10^{-2}$ & Significant ($p < 0.05$) \\
Relevancy    & 72{,}733.00 & $5.54 \times 10^{-5}$ & Significant ($p < 0.05$) \\
Completeness & 27{,}278.50 & $1.06 \times 10^{-1}$ & Not significant \\
\hline
\end{tabular}
\end{table}

\textbf{Correctness is the most diagnostic quality metric}: it is hardest to satisfy,
most sensitive to the architectural differences under evaluation, and its significant
result ($p = 1.03 \times 10^{-2}$) carries the most interpretive weight.

Relevancy warrants a more cautious reading. It is almost universally high for both
agents whenever a response is attempted; the significant difference is primarily driven
by cases where the baseline fails to retrieve any relevant documents and produces no
substantive answer.

The absence of a significant completeness result ($p = 0.106$) is explained by reduced
statistical power rather than an absent effect. As described in
Section~\ref{sec:dataset-limitations}, completeness is evaluated only on factual
queries, which reduces the effective sample to roughly 60\% of that available for
correctness. The directional trend and mean difference are consistent with correctness
--- both point in the same direction --- but the smaller sample is insufficient to clear
the significance threshold. Importantly, Levene's test finds a significant reduction in
completeness \emph{variance} ($p = 0.006$), confirming that the distribution shapes
genuinely differ between architectures. The most accurate interpretation is that the
evaluation lacks the power to confirm the completeness mean shift at this sample size,
not that the shift is absent.

\paragraph{Resource metrics.}
Neither input token usage ($U = 71{,}961$, $p = 0.859$) nor inference duration
($U = 69{,}100$, $p = 0.538$) differs significantly between configurations. Mean input
token counts are comparable (custom: 79{,}319 $\pm$ 82{,}405; baseline:
75{,}395 $\pm$ 78{,}670) and mean durations are within one second of each other
(custom: 39.8 s $\pm$ 27.2 s; baseline: 39.6 s $\pm$ 28.9 s). The custom agent achieves
its quality improvements without imposing a measurable additional cost in tokens or latency.

% -------------------------------------------------------
\subsection{Levene's Test for Equality of Variance}
% -------------------------------------------------------

While the Mann-Whitney U test establishes that the custom agent's scores tend to be
stochastically \emph{higher}, it says nothing about the \emph{spread} of those scores.
Levene's test for equality of variance addresses this directly: it tests whether the
two configurations exhibit significantly different score variability, without assuming
normality. The results are presented in Table~\ref{tab:levene}.

\begin{table}[H]
\centering
\caption{Levene's test results for equality of variance (custom vs.\ baseline+RAG).
         A significant result ($p < 0.05$) indicates the two configurations differ in
         score variability.}
\label{tab:levene}
\begin{tabular}{lrrl}
\hline
\textbf{Metric} & \textbf{Levene statistic} & \textbf{p-value} & \textbf{Result} \\
\hline
Correctness         & 18.38 & $< 0.001$ & Significant \\
Relevancy           & 11.56 & $0.0007$  & Significant \\
Completeness        &  7.76 & $0.0056$  & Significant \\
BERTScore F1        &  7.41 & $0.0066$  & Significant \\
Sentence-BERT sim.  & 16.13 & $< 0.001$ & Significant \\
\hline
Duration (s)        &  0.25 & $0.620$   & Not significant \\
Input tokens        &  0.02 & $0.885$   & Not significant \\
Self-consistency    &  0.06 & $0.810$   & Not significant \\
\hline
\end{tabular}
\end{table}

The test returns a significant result for all five quality metrics, including the
complementary BERTScore and sentence-BERT similarity where the mean difference is small.
This means the custom agent is not only more accurate on average --- it is
\emph{more consistent}. The baseline's distribution is significantly wider across all
quality dimensions, with a heavier low-scoring tail representing cases of partial or
failed retrieval. The custom agent's reduced variance implies more predictable, reliable
responses: fewer failures at the low end and less dependence on whether retrieval happens
to surface the right chunks for a given query.

By contrast, variance in inference duration, input token counts, and self-consistency
scores does not differ significantly between configurations. The resource overhead of the
two agents is therefore comparable not only in expectation but also in spread, and
self-consistency variability is similarly matched. The reliability advantage of the
custom agent is specific to output quality, not a general property of all metrics.

In a legal setting, this reliability dimension may be as practically significant as the
mean improvement. A system that occasionally fails unpredictably is harder to trust and
supervise than one that performs steadily, even if its average score is similar.

% -------------------------------------------------------
\subsection{Self-consistency}
% -------------------------------------------------------

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/self_consistency_violin.png}
    \caption{Distribution of self-consistency scores by agent type. The custom agent
             shows a higher median and narrower interquartile range.}
    \label{fig:self-consistency-violin}
\end{figure}

Self-consistency scores reinforce the pattern seen in the DeepEval metrics visually:
the custom agent exhibits a higher median and a narrower interquartile range than the
baseline, suggesting more reproducible responses across sessions. However, a one-sided
Mann-Whitney U test does not confirm a statistically significant advantage
($U = 7{,}039$, $p = 0.875$). Similarly, Levene's test finds no significant difference
in self-consistency variance between the two configurations (statistic $= 0.06$,
$p = 0.810$). The descriptive pattern is therefore directionally consistent with the
quality findings but does not reach significance, likely due to the smaller number of
observations available at the session-group level.

% -------------------------------------------------------
\section{Summary by Research Question}
\label{sec:rq-summary}
% -------------------------------------------------------

Table~\ref{tab:rq-summary} summarises the empirical evidence for each research question.

\begin{table}[H]
\centering
\caption{Research question outcomes based on empirical results.}
\label{tab:rq-summary}
\begin{tabular}{p{0.08\textwidth} p{0.45\textwidth} p{0.35\textwidth}}
\hline
\textbf{RQ} & \textbf{Finding} & \textbf{Evidence} \\
\hline
Main & Custom agent improves factual accuracy without significant cost increase &
  Correctness significant ($p{=}1.03{\times}10^{-2}$); token usage and latency
  statistically equivalent \\[6pt]
RQ1 & Custom agent significantly outperforms baseline on summary (cross-document) queries;
  factual query gap is smaller but consistent &
  Correctness gap $+14.4$pp on summary queries ($p{=}9.3{\times}10^{-3}$);
  relevancy gap $+8.7$pp ($p{=}2.9{\times}10^{-5}$) \\[6pt]
RQ2 & Indicative evidence from TOSL-2024-125319: gap widens in later sessions; not
  conclusive with current sample &
  Session trajectories; wider gap in sessions 3--4 on authentic case only \\[6pt]
RQ3 & Structured context amplifies smaller model performance; the gap between custom
  and baseline agents narrows as model capability increases &
  LLM barplot: gap is widest for smaller models, negligible for the most capable model \\
\hline
\end{tabular}
\end{table}

\paragraph{Main RQ.}
The results support an affirmative answer: the custom agent achieves statistically
significant improvements in correctness and relevancy over the RAG-only baseline, while
consuming a statistically equivalent number of tokens and producing responses within the
same latency envelope. The quality gain is not purchased at a proportional resource cost.

\paragraph{RQ1.}
The query-type breakdown (Section~\ref{sec:qtype-results}) provides direct empirical
support for RQ1. The custom agent's correctness advantage on summary queries ---
which require cross-document synthesis by definition --- is $+14.4$ percentage points
and statistically significant ($p = 9.3 \times 10^{-3}$). On factual queries, where
a single retrieved chunk can often supply the answer, the gap narrows to $+1.9$pp.
The concentration of the advantage in precisely the query type that stresses naive
RAG most directly answers RQ1 affirmatively: structured extraction mitigates the
cross-document retrieval limitations of chunk-based RAG.

\paragraph{RQ2.}
Session-level trajectories show that the custom agent maintains quality across all
sessions with narrower variance, while the baseline exhibits wider swings and a less
stable profile. This supports the conclusion that the structured representation scales
with document volume rather than degrading under it.

\paragraph{RQ3.}
The model-level breakdown provides the clearest evidence for RQ3: the custom agent's
structured context delivers the largest marginal gain for smaller and less capable
models, with the gap shrinking to near-zero for the most advanced model tested. This
suggests that the factsheet acts as a capability compensator --- partially substituting
for the reasoning capacity that larger models supply natively.
