[project]
name = "master-thesis"
version = "0.1.0"
description = "Master-Thesis project for Sigvard Bratlie"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.uv.workspace]
members = [
    "ui",
    "agent",
]
